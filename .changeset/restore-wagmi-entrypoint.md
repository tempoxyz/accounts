---
'accounts': patch
---

Deprecated `accounts/wagmi`. Consumers should use the following entrypoints instead: `@wagmi/core/tempo`, `wagmi/tempo`, or `wagmi/connectors` (when they only need `tempoWallet`).
