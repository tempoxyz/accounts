---
"accounts": patch
---

Added a React Privy adapter at `accounts/react/privy`, backed Privy Ethereum signing with `secp256k1_sign`, and stopped exporting the lower-level Core JS Privy adapter from the root package entrypoint.
