---
'accounts': minor
---

Updated `viem` (`>=2.54.0`) and `ox` (`~0.14.30`) for the config-id-free TIP-1061 multisig format, removing `genesisConfigId` from the multisig relay `ResolveConfig`, `Operation`, and `Status` shapes.
