---
"accounts": patch
---

Added `postMessage()` and `tempoWalletPostMessage()` adapters (also exported from `accounts/postMessage`) that forward wallet RPC over a Wata postMessage session to a wallet page mounted in an overlay iframe or popup.
