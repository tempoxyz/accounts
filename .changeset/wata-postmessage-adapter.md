---
"accounts": patch
---

Added `postMessage()` and `tempoWalletPostMessage()` adapters, also available from `accounts/postMessage` as `postMessage` and `tempoWallet`. Each request opens the wallet's post-message page in a popup and forwards the wallet RPC through a Wata postMessage session.
