---
"accounts": patch
---

Added an `accessKey` option group to `Provider.create` — `accessKey.keystores` for pluggable per-key-type access-key keystores and `accessKey.authorize` superseding the now-deprecated top-level `authorizeAccessKey`.
