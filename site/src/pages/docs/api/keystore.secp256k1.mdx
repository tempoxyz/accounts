---
title: Keystore.secp256k1
description: Pure-JS secp256k1 keystore.
---

# `Keystore.secp256k1`

Creates a pure-JS secp256k1 [`Keystore`](/docs/api/keystore). The private key is held as hex in the handle, so at-rest protection is the storage adapter and signing runs in JavaScript.

Access keys default to p256, so secp256k1 is used only when requested explicitly via `keyType: 'secp256k1'` — useful because secp256k1 signatures are the chain's cheapest envelope.

## Usage

```ts twoslash
import { Keystore, Provider } from 'accounts'

const provider = Provider.create({
  accessKey: {
    keystores: { secp256k1: Keystore.secp256k1() }, // [!code focus]
  },
})
```
