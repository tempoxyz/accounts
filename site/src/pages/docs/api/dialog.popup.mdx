---
title: Dialog.popup
description: Open the Tempo Wallet auth UI in a popup window.
---

# `Dialog.popup`

Opens the auth app in a new browser window. Used as the default on Safari and insecure (HTTP) origins.

## Usage

```ts
import { dialog, Dialog, Provider } from 'accounts'

const provider = Provider.create({
  adapter: dialog({ dialog: Dialog.popup() }),
})
```

## Parameters

### size

- **Type:** `{ width: number; height: number }`
- **Default:** `{ width: 360, height: 440 }`

Popup window dimensions.

## Behavior

- Tempo Wallet owns the popup UI and signing flow.
- The app owns the opener state, popup-blocked error path, and post-approval success state.
- Production apps should keep the popup tied to a direct user action so browsers do not block it.

## Related guides

- [Tempo Wallet adapter](/docs/adapters/tempo-wallet)
- [Production checklist](/docs/production)
